# 更新记录

## 1.4.0

- 新增独立的“检测路由器”窗口，通过 SSH 读取型号、分区容量和起始位置，给出与转换目标的匹配建议。
- 自动填入当前以太网或 Wi-Fi 网关；支持手动修改地址、选择其他网关和设置 SSH 端口。
- 检测完全可选，不影响固件转换，也不会修改路由器配置或分区。
- 同时核对设备树与实际 MTD 信息，区分 AX6000 stock、110 MiB 单 UBI 和其他布局；信息不足时不作匹配结论。
- SSH 密码默认明文显示，不保存到设置或报告；首次连接核对设备指纹，支持超时与取消。
- 直接读取 SSH 数据，无需路由器提供 base64 等编码工具。

## 1.3.3

- 启动或返回初始页时，不再自动聚焦“选择固件”按钮；保留键盘导航时的焦点提示。

## 1.3.2

- 初始页集中展示“选择固件”入口，支持拖入文件，选好后展开配置。
- 初始窗口收紧高度，隐藏空表单、进度条和暂不可用的操作。
- 清空固件路径后返回初始页，保留填写的设置。
- 修正自动识别模式下误显示“已使用手动选择”的提示。
- 项目主页加入软件截图，提供 Release 下载入口。
- 版本标签自动触发编译、自检和 Release 发布，附带 SHA-256 校验文件。

## 1.3.1

- “设备信息”页签去掉“可选”，两个页签统一宽度。
- 移除页签右侧“设置只写入本次生成的固件”提示。

## 1.3.0

- 统一使用 Kwrt Studio 名称。
- 网络设置和设备信息分为两个页签。
- 统一圆角输入框、焦点边框、按钮悬停和选中状态。
- 未识别网络模式前暂不显示拨号和无线输入框；自动识别保留选中状态并显示识别结果，手动模式单独选择。
- 插件列表显示安装状态颜色、交替行背景，支持清空搜索和自适应列宽。
- 不支持转换的型号继续显示明确提示，仍可查看受支持的软件包列表。
- 加入示例配置和 Windows 自动构建流程。

## 1.2.0

- 识别旁路由，加入网关、DNS 和 DHCP 初始化。
- 加入主机名、自定义签名及 KWRT 作者链接开关。
- 支持查看和搜索 opkg / apk 软件包记录。
